import React from 'react'
import ReactDOM from 'react-dom/client'
import VirtuosaStore from '../virtuosa-loja.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VirtuosaStore />
  </React.StrictMode>,
)
